{
  "title": "The Simple Way",
  "subtitle": "For when you need prompt action on key input",
  "slideType": "LEFT_TEXT_RIGHT_CODE",
  "leftBullets": [
    {
      "title": "Direct Event Access",
      "description": "The `onKeyEvent` callback provides a raw `KeyEvent`."
    },
    {
      "title": "Requires focus",
      "description": "The listener's child (or one of its descendants) must have focus to receive any events."
    },
    {
      "title": "Handles or Ignores",
      "description": "`KeyEventResult.handled` to consume, `KeyEventResult.ignored` to let it bubble up."
    },
    {
      "title": "Bypasses Actions",
      "description": "This is the low-level alternative and does not use the `Intents` system."
    },
    {
      "title": "Wrapper over Focus",
      "description": "Under the hood, it uses a `Focus` widget"
    }
  ],
  "codeSamples": [
    {
      "title": "line ~147: settings_page.dart",
      "code": "onKeyEvent: (FocusNode node, KeyEvent event) {\n    // Handling key press in place to avoid bubbling to the shortcuts\n    if (event is KeyUpEvent) return KeyEventResult.ignored;\n    if (event.logicalKey == LogicalKeyboardKey.enter) {\n      widget.onPressed();\n      return KeyEventResult.handled;\n    }\n    if (event.logicalKey == LogicalKeyboardKey.numpadEnter) {\n      widget.onPressed();\n      return KeyEventResult.handled;\n    }\n    return KeyEventResult.ignored;\n},"
    }
  ]
}
